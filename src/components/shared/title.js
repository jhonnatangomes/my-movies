import styled from 'styled-components';

const Title = styled.p`
    font-weight: 700;
    font-size: 24px;
    line-height: 36px;
    letter-spacing: 0.02em;
    margin-top: 36px;
    margin-bottom: 20px;
`;

export default Title;
